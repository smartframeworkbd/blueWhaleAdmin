import LoadingPage from '@/components/LoadingPage';
import React from 'react';

const Loading = () => {
    return (
        <div>
           <LoadingPage/>
        </div>
    );
}

export default Loading;
